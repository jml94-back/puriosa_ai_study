Day1

Conda 환경 설정
conda create -n my_env python=3.12
conda activate my_env
conda deactivate
conda remove -n my_env
conda env list

pip list
pip install 
pip uninstall

tensorflow
scikit-learn

Day2

0차원   데이터 1개  스칼라
1차원   []        벡터
2차원   [[],[]]   행렬
3차원 이상         텐서

Day3

mse: (원래 값 - 각 예측값) 제곱 하고 다 더해서 n빵
    제곱 하니까 1 미만의 값은 과소, 값이 클수록 과대 해질 듯?

Day4

loss = error = 오차 = cost

열 컬럼 특성 속성 피처 어트리뷰트

Day 5

회귀 모델 실습 정리

Day 6

fit(verbose)
0 : 침묵
1 : 디폴트
2 : 프로그래스바 삭제
3이상 : 에포만 나옴

데이터 분리 train / (new) validation / test

Day 7
분류 - 이진분류, 다중분류
분류시 y 값 확인 필요
train_test_split( stratify = y) y를 값에 따라 같은 비율로 나눔


Day 8
다중분류
OneHot Encoding to_categorical(keras)

relu - 배치 노말라이제이션

Day 12

gpu 사용 설정
1. nvidia driver 설치
2. cuda 11.2.2
3. cudnn 8.1 - 고급 - visual stuidio Intigration 에러 발생
4. 가상환경 추가 ( conda create -n tf29x-gpu python=3.10 )
5. python 3.10.x
6. tensorflow 2.9.x ( pip install tensorflow-gpu==2.9.3 )
numpy 1.26.4으로 버전 다운
pip install tensorflow-gpu==1.26.4
pip install scikit-learn==1.7.2

버전 확인
nvidia-smi
nvcc -V

DNN csv 2차원
RNN     3차원
CNN 